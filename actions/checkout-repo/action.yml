name: "Checkout Repository"
description: "Checkout repository with Liquibase demo token"
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v7
      with:
        token: ${{ inputs.token }}
inputs:
  token:
    description: "GitHub token"
    required: true